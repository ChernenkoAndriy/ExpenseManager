<Page x:Class="ExpenseManager.UI.EditWalletPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseManager.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Редагування гаманця">

    <Grid Margin="30">
        <StackPanel MaxWidth="400" HorizontalAlignment="Left">
            <TextBlock x:Name="PageTitle" Text="Редагувати гаманець" Style="{StaticResource PageTitleStyle}"/>

            <TextBlock Text="Назва гаманця:" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Валюта:" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox x:Name="CurrencyComboBox" 
                      SelectedItem="{Binding Currency}" 
                      ItemsSource="{Binding AvailableCurrencies}"/>

            <UniformGrid Columns="2" Margin="0,20,0,0">
                <Button Content="Скасувати" Click="CancelButton_Click" Style="{StaticResource DangerButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Зберегти" Click="SaveButton_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Page>