<Page x:Class="ExpenseManager.UI.TransactionDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseManager.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TransactionDetailsPage">

    <Grid Margin="30">
        <StackPanel MaxWidth="400" HorizontalAlignment="Left">
            <Button Content="← Back" Click="BackButton_Click" HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,20"/>

            <TextBlock Text="Transaction Details" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <Border BorderBrush="LightGray" BorderThickness="1" Padding="20" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Amount:" Foreground="Gray" Margin="0,5,0,0"/>
                    <TextBlock x:Name="AmountText" FontSize="20" FontWeight="Bold" Text="0.00"/>

                    <TextBlock Text="Category:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock x:Name="CategoryText" FontSize="18" Text="Food"/>

                    <TextBlock Text="Date and Time:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock x:Name="DateText" FontSize="16" Text="01.01.2024 12:00"/>

                    <TextBlock Text="Comment:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock x:Name="DescriptionText" FontSize="16" TextWrapping="Wrap" Text="No comment"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>
