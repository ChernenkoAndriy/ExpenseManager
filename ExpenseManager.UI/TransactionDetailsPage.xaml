<Page x:Class="ExpenseManager.UI.TransactionDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseManager.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TransactionDetailsPage">

    <Grid Margin="30">
        <StackPanel MaxWidth="400" HorizontalAlignment="Left">
            <Button Content="← Назад" Click="BackButton_Click" HorizontalAlignment="Left" Padding="10,5" Margin="0,0,0,20"/>

            <TextBlock Text="Деталі транзакції" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

            <Border BorderBrush="LightGray" BorderThickness="1" Padding="20" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="ID Транзакції:" Foreground="Gray"/>
                    <TextBlock Text="{Binding Id}" FontSize="14" Margin="0,0,0,10"/>

                    <TextBlock Text="Сума:" Foreground="Gray"/>
                    <TextBlock x:Name="AmountText" FontSize="20" FontWeight="Bold" Text="{Binding Amount, StringFormat={}{0:N2}}"/>

                    <TextBlock Text="Тип:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock Text="{Binding TransactionType}" FontSize="16"/>

                    <TextBlock Text="Категорія:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock FontSize="18" Text="{Binding Category, Converter={StaticResource EnumConverter}}"/>

                    <TextBlock Text="Дата та час:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock FontSize="16" Text="{Binding FormattedDate}"/>

                    <TextBlock Text="Коментар:" Foreground="Gray" Margin="0,15,0,0"/>
                    <TextBlock FontSize="16" TextWrapping="Wrap" Text="{Binding Description}"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</Page>