<UserControl x:Class="ExpenseManager.UI.Views.WalletDetailsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" Content="← До списку" Command="{Binding BackCommand}" 
                HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Foreground="Blue" Margin="0,0,0,10"/>

        <DockPanel Grid.Row="1" Margin="0,0,0,20">
            <StackPanel DockPanel.Dock="Left">
                <TextBlock Text="{Binding Wallet.Name}" FontSize="24" FontWeight="Bold"/>

                <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                    <TextBlock Text="Поточний баланс: " FontSize="16" Foreground="Gray"/>
                    <TextBlock Text="{Binding Wallet.TotalBalance, StringFormat={}{0:N2}}" 
                   FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text=" " FontSize="16"/>
                    <TextBlock Text="{Binding Wallet.Currency}" FontSize="16"/>
                </StackPanel>
            </StackPanel>
            <Button Content="+ Додати транзакцію" Command="{Binding AddTransactionCommand}" 
                    Background="#28A745" Foreground="White" Padding="15,8" FontWeight="Bold" DockPanel.Dock="Right" HorizontalAlignment="Right" VerticalAlignment="Center"/>
        </DockPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Wallet.Transactions}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding FormattedDate}"/>
                <DataGridTextColumn Header="Категорія" Binding="{Binding Category, Converter={StaticResource EnumConverter}}"/>

                <DataGridTemplateColumn Header="Сума">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Amount, StringFormat={}{0:N2}}" FontWeight="Bold">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="Green"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Amount, Converter={StaticResource EnumConverter}, ConverterParameter=IsNegative}" Value="True">
                                                <Setter Property="Foreground" Value="Red"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Опис" Binding="{Binding Description}" Width="*"/>

                <DataGridTemplateColumn Header="Дії">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Деталі" 
                        Command="{Binding DataContext.ViewTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" 
                        Margin="0,0,5,0" Padding="10,2"/>

                                <Button Content="Видалити" Command="{Binding DataContext.DeleteTransactionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                        CommandParameter="{Binding}" Foreground="Red" Padding="10,2"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>