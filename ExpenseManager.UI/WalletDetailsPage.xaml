<Page x:Class="ExpenseManager.UI.WalletDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseManager.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Деталі гаманця">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Button x:Name="BackButton" Content="← До списку" 
                    HorizontalAlignment="Left" Click="BackButton_Click"/>
            <Button Content="+ Додати транзакцію" HorizontalAlignment="Right" 
                    Style="{StaticResource PrimaryButtonStyle}" Click="AddTransaction_Click"/>
        </DockPanel>

        <StackPanel Grid.Row="1" Margin="0,20,0,10">
            <TextBlock x:Name="WalletNameText" Text="Назва гаманця" Style="{StaticResource PageTitleStyle}" Margin="0,0,0,5"/>
            <TextBlock x:Name="WalletBalanceText" Text="Баланс: 0.00" FontSize="18" FontWeight="SemiBold"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="TransactionsDataGrid" 
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,10,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding FormattedDate}" Width="130"/>
                <DataGridTemplateColumn Header="Категорія" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Category, Converter={StaticResource EnumConverter}}" VerticalAlignment="Center" Margin="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Сума" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTextColumn Header="Опис" Binding="{Binding Description}" Width="*"/>
                <DataGridTemplateColumn Header="Дії" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Деталі" Click="TransactionDetails_Click" Margin="0,0,5,0"/>
                                <Button Content="Видалити" Style="{StaticResource DangerButtonStyle}" Click="DeleteTransaction_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>