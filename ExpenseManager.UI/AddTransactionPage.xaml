<Page x:Class="ExpenseManager.UI.AddTransactionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ExpenseManager.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Додати транзакцію">

    <Grid Margin="30">
        <StackPanel MaxWidth="400" HorizontalAlignment="Left">
            <TextBlock Text="Нова транзакція" Style="{StaticResource PageTitleStyle}"/>

            <TextBlock Text="Сума (від'ємна для витрат):" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"/>

            <TextBlock Text="Категорія:" Style="{StaticResource FieldLabelStyle}"/>
            <ComboBox x:Name="CategoryComboBox" 
                      SelectedItem="{Binding Category}" 
                      ItemsSource="{Binding AvailableCategories}">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Converter={StaticResource EnumConverter}}"/>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBlock Text="Опис:" Style="{StaticResource FieldLabelStyle}"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>

            <UniformGrid Columns="2" Margin="0,20,0,0">
                <Button Content="Скасувати" Click="CancelButton_Click" Style="{StaticResource DangerButtonStyle}" Margin="0,0,10,0"/>
                <Button Content="Зберегти" Click="SaveButton_Click" Style="{StaticResource PrimaryButtonStyle}"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Page>